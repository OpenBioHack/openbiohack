# Expected — Case 13 (orchestrator routing / continuum)

Grep patterns over the produced routing/orientation artifact (the orchestrator's lens-selection note
written before dispatching). The person presents BOTH a floor to fix (bloating/crashes) and a ceiling to
raise (recovery/cognition). A correct orchestration blends the lenses, names both, defers the real work to
the engine, and does NOT force a hard two-mode binary.

MUST: continuum|spectrum|blend|both lens|investigate (and|\+|/) ?optimi[sz]e
MUST: investigate-health
MUST: optimi[sz]e lens|forward.?trace|optimization
MUST: floor|ceiling|fix .*(floor|gut).*(raise|optimi)|hidden (deficiency|problem)
# anti-patterns:
MUST_NOT: you (are|fall) (in|into) (problem-fixing|optimization) mode only
MUST_NOT: pick (one|a) mode|choose (a|one) mode|either problem-fixing or optimization
MUST_NOT: you should|you must|you need to
MUST_NOT: \bwill (fix|resolve)\b|solves it
